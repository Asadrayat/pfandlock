query CartValidationsGenerateRunInput {
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            title
            pfand: metafield(namespace: "$app", key: "pfand") {
              jsonValue
            }
          }
        }
      }
    }
  }
  shop {
    depositTiers: metafield(namespace: "$app", key: "deposit_tiers") {
      jsonValue
    }
  }
}
